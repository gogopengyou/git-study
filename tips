Title: git 本地分支与远程分支

github上已经有master分支 和dev分支

#####################
在本地

git checkout -b dev 新建并切换到本地dev分支

git pull origin dev 本地分支与远程分支相关联

在本地新建分支并推送到远程

git checkout -b test

git push origin test   这样远程仓库中也就创建了一个test分支



#####################
other:
git push --all origin
将所有本地分支都推送到origin主机

git push --force origin
导致在远程主机产生一个”非直进式”的合并(non-fast-forward merge)。除非你很确定要这样做，否则应该尽量避免使用–force选项。

cherry-pick ~
rebase ~



